node_modules
dist
.playtest

# Personal music copy — kept local so a published build never redistributes it.
public/music/*.mp3

# scratch bundles from ad-hoc engine probes
.scratch/

# ad-hoc engine probe bundles (agents and manual checks)
.probe/
.verify/
.scratch/
