{
  "name": "abyss-trainer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js",
    "playtest": "esbuild src/engine/sim.ts --bundle --format=esm --outfile=.playtest/sim.mjs --log-level=error && esbuild src/bosses/registry.ts --bundle --format=esm --outfile=.playtest/registry.mjs --log-level=error && node test/playtest.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "esbuild": "^0.21.5"
  }
}
